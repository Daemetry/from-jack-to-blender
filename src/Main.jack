class Main
{
    function void main()
    {
        /*var Fraction f;
        let f = Fraction.new(2, 15); 
        do f.print();
        f.dispose(); */
        do Output.printInt(10);
        return;
    }
}